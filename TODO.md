# TODO

Generated by the weekly docs verification workflow. Last updated: 2026-07-15.
Items are carried forward, re-prioritized, or dropped each Monday based on the
current state of the codebase.

## P1 - Correctness and blockers

- [ ] README.md is a stale 2-line stub from the upstream prompts collection; rewrite it to describe this build (link PRODUCT.md, ARCHITECTURE.md, CLAUDE.md).
- [ ] claude.yml sits at the repo root, so the @claude responder never runs; move it to .github/workflows/claude.yml (requires the ANTHROPIC_API_KEY repository secret).
- [ ] Add the CLAUDE_CODE_OAUTH_TOKEN repository secret and trigger .github/workflows/update-claude-md.yml via workflow_dispatch to confirm the Monday pass works end to end.

## P2 - In-flight and target work

- [ ] Module 2 (target): create the vault skeleton the agent briefs assume - Resources/Processed_Transcripts, Resources/Hubs, Resources/Concepts, Resources/Technologies, Resources/Script_Frameworks, Archives/Transcripts/Raw - or document where the vault lives outside the repo.
- [ ] Module 2 (target): process the 5 transcripts in Inbox/Transcripts_to_Process/ with the transcript processing agent (brief: obisidian-knowledge-graph/Agents/Agents/transcript_processing_agent.md) and archive the raw files.
- [ ] Module 2 (target): run the vault connectivity agent after the first processed transcripts land to seed concept/hub/technology nodes.
- [ ] Module 2: decide the note-import path for the 15 Inbox/Web_to_Process/ and 28 Inbox/Skills_to_Process/ notes (which agent consumes them, and into which Resources/ folder) - currently no brief claims them.
- [ ] Module 3 (target): implement the content writing stage around notion-db-to-technical-blog/notion-db-to-technical-blog.md, including the structured content object source and the blog target; record stack choices in a module-3 *-plan.md.
- [ ] Module 4 (target): implement the ADR half of the self-documenting agent (brief: obisidian-knowledge-graph/Agents/Agents/self-documenting-ai-agent/README.md); the brief's template path .claude/adr-template.md is gitignored, so pick a tracked location such as docs/adr/adr-template.md and create docs/adr/.
- [ ] Write the per-module *-plan.md files CONTRIBUTING.md's workflow requires (none exist yet for modules 2-4).

## P3 - Hygiene and hardening

- [ ] Add smoke tests for the pure functions in scripts/ (extract_video_id in fetch_transcripts.py; parse_price/parse_curriculum in scrape_courses.py; split_frontmatter/field in extract_skills.py).
- [ ] scrape_courses.py and fetch_transcripts.py shell out to curl as a TLS workaround; document the curl requirement or gate the fallback behind a flag.
- [ ] extract_skills.py fails on fresh clones because .claude/skills/ is gitignored; print a setup hint pointing at github.com/microsoft/azure-skills when SKILLS_DIR is missing.
- [ ] Decide whether create-app.html (static "Create Claude Code GitHub App" helper page) belongs in this repo; document or remove it.
- [ ] ARCHITECTURE.md references the author-local path /Users/robertjames/loc/repos/obsidian-importer; replace with the public project reference so the doc works for other machines.
- [ ] Add a lint/format check for the Python scripts (e.g. ruff) and wire it into a simple CI workflow.

## Done since last week

- Initial TODO.md; no previous week to reconcile.
- Added requirements.txt (youtube-transcript-api pin), referenced it in CONTRIBUTING.md setup and README.md pre-requisites, and wired it into the weekly verification workflow.
